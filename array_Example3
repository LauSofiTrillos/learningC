//Find de largest element in the array 
#include <stdio.h>
  int main(){
   int arr[5] ={3, -4, -8, 9, 1};
    for(int i=0; i<5; ++i){
     if(arr[0] < arr[i]){
        arr[0] = arr[i];
     }
    }
   printf("largest element = %d", arr[0]); //9
    return 0;
}

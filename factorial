#include <stdio.h>
//Fatorial of a Number 
int main(){
  int n=10;
  long int result = 1;
  
  if(n<0){
  printf("Error!")
  }
  else{
      for(int i = 2; i <= n; ++i){
         result *= i;    
   }
  printf("%d! =  %li", n, result);
  }
return 0;
}

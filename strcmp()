// strcmp() compara dos strings
#include <stdio.h>
#include <string.h>
  char str1[] = "a,b,c,d", str2[] = "cdEA";
  int result;
  
  result = strcmp(str1, str2);
  printf("Result = %d\n", result);
  
  if(result == 0){
   printf(st)
  }

#include <math.h>
#include <stdio.h>

int main(){
    double base = 3.0, exponent = 4.0, result;
    
    //base^exponent
    result = pow(base, exponent);
    
    printf("%.2lf ^ %.2lf = %.2lf", base, exponent, result);
    return 0;
}

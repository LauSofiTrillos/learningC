//ejemplo fgets() y puts()
#include <stdio.h>
int main(){
  char name[30];
  
  //leer string 
  printf("Enter name: ");
  fgets(name, sizeof(name), stdin);
  
  //imprimir string
  printf("Name: ");
  puts(name);
  
  return 0;
}

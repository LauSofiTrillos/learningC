// strcat() sirve para concatenar Strings 
#include <stdio.h>
#include <string.h>

int main(){

  char str1[10] = "C ";
  char str2[10] = "Program";
  
  strcat(str1, str2);
  puts(str1); // C Program 
  puts(str2); // Program 
  
  return 0;
}
// el resultado de concatenar se guarda en str1

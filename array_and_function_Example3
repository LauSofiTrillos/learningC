//Pass a 2D array to a function 
#include <stdio.h>
  void printNum(int n[2][2]){
   printf("Stored Elements: ");
   for(int i = 0; i < 2; ++i){
    for(int j = 0; j < 2; ++j)
    printf("%d", n[i][j]);
    }
   }
  }
int main(){
 int x[2][2]={{3, -2},{9, 8}};
 printNum(x);
 return 0;
 }
